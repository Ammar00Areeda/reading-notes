## what is wireframe

It is a visual guide that shows the framework or structure of any website, and defines the design of the page or the way in which the contents are organized, including interface elements and navigation systems, and how they are integrated; The framework usually does not have a typographical style

## why we need wireframe

The network framework can be used by different disciplines, programmers use it to control significantly the performance of the site, web designers use it to drive the process of user interface UI, and user experience design specialists and information engineers use this framework to show paths Browsing between pages, used by business owners to ensure that requirements and goals are met through the design

## what is HTML

HTML (Hypertext Markup Language) is the code that is used to structure a web page and its content. For example, content could be structured within a set of paragraphs, a list of bulleted points, or using images and data tables. As the title suggests, this article will give you a basic understanding of HTML and its functions.

## anatomy of an HTML elements

the opening tag like <p>
the closisng tag </p>
the content  which in this case, is just text
the element all above 

![elements](https://developer.mozilla.org/en-US/docs/Learn/Getting_started_with_the_web/HTML_basics/grumpy-cat-small.png)

## semantics 
Semantic elements = elements with a meaning.

What are Semantic Elements?
A semantic element clearly describes its meaning to both the browser and the developer.

Examples of non-semantic elements: <div> and <span> - Tells nothing about its content.

Examples of semantic elements: <form>, <table>, and <article> - Clearly defines its content.
![semantic elments](https://www.w3schools.com/html/img_sem_elements.gif)

## semantic in java script

In JavaScript, consider a function that takes a string parameter, and returns an <li> element with that string as its textContent. Would you need to look at the code to understand what the function did if it was called build('Peach'), or createLiWithContent('Peach')?

## semantic in css

In CSS, consider styling a list with li elements representing different types of fruits. Would you know what part of the DOM is being selected with div > ul > li, or .fruits__item?



